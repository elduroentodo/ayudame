"use client";
import {useState} from "react";
export default function Home(){const[q,setQ]=useState("");const[a,setA]=useState("");const[loading,setLoading]=useState(false);async function ask(){setLoading(true);const r=await fetch("/api/chat",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({agentId:"demo",message:q})});const d=await r.json();setA(d.answer||d.error);setLoading(false)}return <main><h1>Ayúdame</h1><p className="muted">Agentes de IA simples para negocios.</p><div className="card"><h2>Probar agente RAG</h2><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="Escribe una pregunta"/><button onClick={ask}>{loading?"Consultando...":"Preguntar"}</button>{a&&<p>{a}</p>}</div><div className="row"><div className="card"><h3>1. Conocimiento</h3><p className="muted">Ingesta textos o contenido extraído de webs, PDF y otros documentos.</p></div><div className="card"><h3>2. Casos de uso</h3><p className="muted">Consulta datos simples y devuelve respuestas estructuradas.</p></div></div></main>}